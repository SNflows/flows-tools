"""
This type stub file was generated by pyright.
"""

import numpy as np
from astropy import config as _config
from astropy.utils.compat import NUMPY_LT_1_20, NUMPY_LT_1_22
from astropy.utils.compat.misc import override__dir__
from astropy.utils.data_info import ParentDtypeInfo

"""
This module defines the `Quantity` object, which represents a number with some
associated units. `Quantity` objects support operations like ordinary numbers,
but will deal with unit conversions internally.
"""
__all__ = ["Quantity", "SpecificTypeQuantity", "QuantityInfoBase", "QuantityInfo", "allclose", "isclose"]
__doctest_skip__ = ...
_UNIT_NOT_INITIALISED = ...
_UFUNCS_FILTER_WARNINGS = ...
class Conf(_config.ConfigNamespace):
    """
    Configuration parameters for Quantity
    """
    latex_array_threshold = ...


conf = ...
class QuantityIterator:
    """
    Flat iterator object to iterate over Quantities

    A `QuantityIterator` iterator is returned by ``q.flat`` for any Quantity
    ``q``.  It allows iterating over the array as if it were a 1-D array,
    either in a for-loop or by calling its `next` method.

    Iteration is done in C-contiguous style, with the last index varying the
    fastest. The iterator can also be indexed using basic slicing or
    advanced indexing.

    See Also
    --------
    Quantity.flatten : Returns a flattened copy of an array.

    Notes
    -----
    `QuantityIterator` is inspired by `~numpy.ma.core.MaskedIterator`.  It
    is not exported by the `~astropy.units` module.  Instead of
    instantiating a `QuantityIterator` directly, use `Quantity.flat`.
    """
    def __init__(self, q) -> None:
        ...
    
    def __iter__(self): # -> Self@QuantityIterator:
        ...
    
    def __getitem__(self, indx): # -> Unknown:
        ...
    
    def __setitem__(self, index, value): # -> None:
        ...
    
    def __next__(self):
        """
        Return the next value, or raise StopIteration.
        """
        ...
    
    next = ...
    def __len__(self): # -> int:
        ...
    
    @property
    def base(self): # -> Unknown:
        """A reference to the array that is iterated over."""
        ...
    
    @property
    def coords(self):
        """An N-dimensional tuple of current coordinates."""
        ...
    
    @property
    def index(self):
        """Current flat index into the array."""
        ...
    
    def copy(self):
        """Get a copy of the iterator as a 1-D array."""
        ...
    


class QuantityInfoBase(ParentDtypeInfo):
    attrs_from_parent = ...
    _supports_indexing = ...
    @staticmethod
    def default_format(val): # -> str:
        ...
    
    @staticmethod
    def possible_string_format_functions(format_): # -> Generator[((format_: Unknown, val: Unknown) -> str) | ((format_: Unknown, val: Unknown) -> Unknown), None, None]:
        """Iterate through possible string-derived format functions.

        A string can either be a format specifier for the format built-in,
        a new-style format string, or an old-style format string.

        This method is overridden in order to suppress printing the unit
        in each row since it is already at the top in the column header.
        """
        ...
    


class QuantityInfo(QuantityInfoBase):
    """
    Container for meta information like name, description, format.  This is
    required when the object is used as a mixin column within a table, but can
    be used as a general way to store meta information.
    """
    _represent_as_dict_attrs = ...
    _construct_from_dict_args = ...
    _represent_as_dict_primary_data = ...
    def new_like(self, cols, length, metadata_conflicts=..., name=...):
        """
        Return a new Quantity instance which is consistent with the
        input ``cols`` and has ``length`` rows.

        This is intended for creating an empty column object whose elements can
        be set in-place for table operations like join or vstack.

        Parameters
        ----------
        cols : list
            List of input columns
        length : int
            Length of the output column object
        metadata_conflicts : str ('warn'|'error'|'silent')
            How to handle metadata conflicts
        name : str
            Output column name

        Returns
        -------
        col : `~astropy.units.Quantity` (or subclass)
            Empty instance of this class consistent with ``cols``

        """
        ...
    
    def get_sortable_arrays(self): # -> list[Unknown | None]:
        """
        Return a list of arrays which can be lexically sorted to represent
        the order of the parent column.

        For Quantity this is just the quantity itself.


        Returns
        -------
        arrays : list of ndarray
        """
        ...
    


class Quantity(np.ndarray):
    """A `~astropy.units.Quantity` represents a number with some associated unit.

    See also: https://docs.astropy.org/en/stable/units/quantity.html

    Parameters
    ----------
    value : number, `~numpy.ndarray`, `~astropy.units.Quantity` (sequence), or str
        The numerical value of this quantity in the units given by unit.  If a
        `Quantity` or sequence of them (or any other valid object with a
        ``unit`` attribute), creates a new `Quantity` object, converting to
        `unit` units as needed.  If a string, it is converted to a number or
        `Quantity`, depending on whether a unit is present.

    unit : unit-like
        An object that represents the unit associated with the input value.
        Must be an `~astropy.units.UnitBase` object or a string parseable by
        the :mod:`~astropy.units` package.

    dtype : ~numpy.dtype, optional
        The dtype of the resulting Numpy array or scalar that will
        hold the value.  If not provided, it is determined from the input,
        except that any integer and (non-Quantity) object inputs are converted
        to float by default.

    copy : bool, optional
        If `True` (default), then the value is copied.  Otherwise, a copy will
        only be made if ``__array__`` returns a copy, if value is a nested
        sequence, or if a copy is needed to satisfy an explicitly given
        ``dtype``.  (The `False` option is intended mostly for internal use,
        to speed up initialization where a copy is known to have been made.
        Use with care.)

    order : {'C', 'F', 'A'}, optional
        Specify the order of the array.  As in `~numpy.array`.  This parameter
        is ignored if the input is a `Quantity` and ``copy=False``.

    subok : bool, optional
        If `False` (default), the returned array will be forced to be a
        `Quantity`.  Otherwise, `Quantity` subclasses will be passed through,
        or a subclass appropriate for the unit will be used (such as
        `~astropy.units.Dex` for ``u.dex(u.AA)``).

    ndmin : int, optional
        Specifies the minimum number of dimensions that the resulting array
        should have.  Ones will be pre-pended to the shape as needed to meet
        this requirement.  This parameter is ignored if the input is a
        `Quantity` and ``copy=False``.

    Raises
    ------
    TypeError
        If the value provided is not a Python numeric type.
    TypeError
        If the unit provided is not either a :class:`~astropy.units.Unit`
        object or a parseable string unit.

    Notes
    -----
    Quantities can also be created by multiplying a number or array with a
    :class:`~astropy.units.Unit`. See https://docs.astropy.org/en/latest/units/

    Unless the ``dtype`` argument is explicitly specified, integer
    or (non-Quantity) object inputs are converted to `float` by default.
    """
    _equivalencies = ...
    _default_unit = ...
    _unit = ...
    __array_priority__ = ...
    def __class_getitem__(cls, unit_shape_dtype): # -> Unit | PhysicalType:
        """Quantity Type Hints.

        Unit-aware type hints are ``Annotated`` objects that encode the class,
        the unit, and possibly shape and dtype information, depending on the
        python and :mod:`numpy` versions.

        Schematically, ``Annotated[cls[shape, dtype], unit]``

        As a classmethod, the type is the class, ie ``Quantity``
        produces an ``Annotated[Quantity, ...]`` while a subclass
        like :class:`~astropy.coordinates.Angle` returns
        ``Annotated[Angle, ...]``.

        Parameters
        ----------
        unit_shape_dtype : :class:`~astropy.units.UnitBase`, str, `~astropy.units.PhysicalType`, or tuple
            Unit specification, can be the physical type (ie str or class).
            If tuple, then the first element is the unit specification
            and all other elements are for `numpy.ndarray` type annotations.
            Whether they are included depends on the python and :mod:`numpy`
            versions.

        Returns
        -------
        `typing.Annotated`, `typing_extensions.Annotated`, `astropy.units.Unit`, or `astropy.units.PhysicalType`
            Return type in this preference order:
            * if python v3.9+ : `typing.Annotated`
            * if :mod:`typing_extensions` is installed : `typing_extensions.Annotated`
            * `astropy.units.Unit` or `astropy.units.PhysicalType`

        Raises
        ------
        TypeError
            If the unit/physical_type annotation is not Unit-like or
            PhysicalType-like.

        Examples
        --------
        Create a unit-aware Quantity type annotation

            >>> Quantity[Unit("s")]
            Annotated[Quantity, Unit("s")]

        See Also
        --------
        `~astropy.units.quantity_input`
            Use annotations for unit checks on function arguments and results.

        Notes
        -----
        With Python 3.9+ or :mod:`typing_extensions`, |Quantity| types are also
        static-type compatible.
        """
        ...
    
    def __new__(cls, value, unit=..., dtype=..., copy=..., order=..., subok=..., ndmin=...):
        ...
    
    def __array_finalize__(self, obj): # -> None:
        ...
    
    def __array_wrap__(self, obj, context=...): # -> Self@Quantity | Quantity | Any:
        ...
    
    def __array_ufunc__(self, function, method, *inputs, **kwargs): # -> Any | tuple[Unknown, ...] | list[Unknown] | Self@Quantity | Quantity:
        """Wrap numpy ufuncs, taking care of units.

        Parameters
        ----------
        function : callable
            ufunc to wrap.
        method : str
            Ufunc method: ``__call__``, ``at``, ``reduce``, etc.
        inputs : tuple
            Input arrays.
        kwargs : keyword arguments
            As passed on, with ``out`` containing possible quantity output.

        Returns
        -------
        result : `~astropy.units.Quantity`
            Results of the ufunc, with the unit set properly.
        """
        ...
    
    def __quantity_subclass__(self, unit): # -> tuple[Type[Quantity], Literal[True]]:
        """
        Overridden by subclasses to change what kind of view is
        created based on the output unit of an operation.

        Parameters
        ----------
        unit : UnitBase
            The unit for which the appropriate class should be returned

        Returns
        -------
        tuple :
            - `~astropy.units.Quantity` subclass
            - bool: True if subclasses of the given class are ok
        """
        ...
    
    def __deepcopy__(self, memo): # -> Self@Quantity:
        ...
    
    def __reduce__(self): # -> tuple[str, ...]:
        ...
    
    def __setstate__(self, state): # -> None:
        ...
    
    info = ...
    def to(self, unit, equivalencies=..., copy=...): # -> Self@Quantity | Quantity | Any:
        """
        Return a new `~astropy.units.Quantity` object with the specified unit.

        Parameters
        ----------
        unit : unit-like
            An object that represents the unit to convert to. Must be
            an `~astropy.units.UnitBase` object or a string parseable
            by the `~astropy.units` package.

        equivalencies : list of tuple
            A list of equivalence pairs to try if the units are not
            directly convertible.  See :ref:`astropy:unit_equivalencies`.
            If not provided or ``[]``, class default equivalencies will be used
            (none for `~astropy.units.Quantity`, but may be set for subclasses)
            If `None`, no equivalencies will be applied at all, not even any
            set globally or within a context.

        copy : bool, optional
            If `True` (default), then the value is copied.  Otherwise, a copy
            will only be made if necessary.

        See also
        --------
        to_value : get the numerical value in a given unit.
        """
        ...
    
    def to_value(self, unit=..., equivalencies=...): # -> ndarray[Unknown, Unknown] | NDArray[floating[Any]] | float | NDArray[void] | Any:
        """
        The numerical value, possibly in a different unit.

        Parameters
        ----------
        unit : unit-like, optional
            The unit in which the value should be given. If not given or `None`,
            use the current unit.

        equivalencies : list of tuple, optional
            A list of equivalence pairs to try if the units are not directly
            convertible (see :ref:`astropy:unit_equivalencies`). If not provided
            or ``[]``, class default equivalencies will be used (none for
            `~astropy.units.Quantity`, but may be set for subclasses).
            If `None`, no equivalencies will be applied at all, not even any
            set globally or within a context.

        Returns
        -------
        value : ndarray or scalar
            The value in the units specified. For arrays, this will be a view
            of the data if no unit conversion was necessary.

        See also
        --------
        to : Get a new instance in a different unit.
        """
        ...
    
    value = ...
    @property
    def unit(self): # -> <subclass of Unit and StructuredUnit> | StructuredUnit | Unit | UnitBase | None:
        """
        A `~astropy.units.UnitBase` object representing the unit of this
        quantity.
        """
        ...
    
    @property
    def equivalencies(self): # -> list[Unknown]:
        """
        A list of equivalencies that will be applied by default during
        unit conversions.
        """
        ...
    
    @property
    def si(self): # -> Self@Quantity | Quantity | Any:
        """
        Returns a copy of the current `Quantity` instance with SI units. The
        value of the resulting object will be scaled.
        """
        ...
    
    @property
    def cgs(self): # -> Self@Quantity | Quantity | Any:
        """
        Returns a copy of the current `Quantity` instance with CGS units. The
        value of the resulting object will be scaled.
        """
        ...
    
    @property
    def isscalar(self): # -> bool:
        """
        True if the `value` of this quantity is a scalar, or False if it
        is an array-like object.

        .. note::
            This is subtly different from `numpy.isscalar` in that
            `numpy.isscalar` returns False for a zero-dimensional array
            (e.g. ``np.array(1)``), while this is True for quantities,
            since quantities cannot represent true numpy scalars.
        """
        ...
    
    _include_easy_conversion_members = ...
    @override__dir__
    def __dir__(self): # -> set[Unknown] | list[Unknown]:
        """
        Quantities are able to directly convert to other units that
        have the same physical type.  This function is implemented in
        order to make autocompletion still work correctly in IPython.
        """
        ...
    
    def __getattr__(self, attr): # -> float | NDArray[void] | Any:
        """
        Quantities are able to directly convert to other units that
        have the same physical type.
        """
        ...
    
    def __eq__(self, other) -> bool:
        ...
    
    def __ne__(self, other) -> bool:
        ...
    
    def __lshift__(self, other): # -> _NotImplementedType | Self@Quantity:
        ...
    
    def __ilshift__(self, other): # -> _NotImplementedType | Self@Quantity:
        ...
    
    def __rlshift__(self, other): # -> _NotImplementedType | Quantity:
        ...
    
    def __rrshift__(self, other): # -> _NotImplementedType:
        ...
    
    def __rshift__(self, other): # -> _NotImplementedType:
        ...
    
    def __irshift__(self, other): # -> _NotImplementedType:
        ...
    
    def __mul__(self, other): # -> Self@Quantity | Quantity | Any | _NotImplementedType | NDArray[bool_]:
        """ Multiplication between `Quantity` objects and other objects."""
        ...
    
    def __imul__(self, other): # -> Self@Quantity | NDArray[bool_]:
        """In-place multiplication between `Quantity` objects and others."""
        ...
    
    def __rmul__(self, other): # -> Self@Quantity | Quantity | Any | _NotImplementedType | NDArray[bool_]:
        """ Right Multiplication between `Quantity` objects and other
        objects.
        """
        ...
    
    def __truediv__(self, other): # -> Self@Quantity | Quantity | Any | _NotImplementedType | NDArray[float64]:
        """ Division between `Quantity` objects and other objects."""
        ...
    
    def __itruediv__(self, other): # -> Self@Quantity | NDArray[floating[Unknown]]:
        """Inplace division between `Quantity` objects and other objects."""
        ...
    
    def __rtruediv__(self, other): # -> Self@Quantity | Quantity | Any | NDArray[float64]:
        """ Right Division between `Quantity` objects and other objects."""
        ...
    
    def __pow__(self, other): # -> Self@Quantity | Quantity | Any | NDArray[int8]:
        ...
    
    def __hash__(self) -> int:
        ...
    
    def __iter__(self): # -> Generator[Self@Quantity | Quantity | Any | Unknown, None, None]:
        ...
    
    def __getitem__(self, key): # -> Self@Quantity | Quantity | Any | NDArray[Any]:
        ...
    
    def __setitem__(self, i, value): # -> None:
        ...
    
    def __bool__(self): # -> Literal[True]:
        """Quantities should always be treated as non-False; there is too much
        potential for ambiguity otherwise.
        """
        ...
    
    def __len__(self): # -> int:
        ...
    
    def __float__(self): # -> float:
        ...
    
    def __int__(self) -> int:
        ...
    
    def __index__(self): # -> Any:
        ...
    
    def to_string(self, unit=..., precision=..., format=..., subfmt=...): # -> Any | str:
        """
        Generate a string representation of the quantity and its unit.

        The behavior of this function can be altered via the
        `numpy.set_printoptions` function and its various keywords.  The
        exception to this is the ``threshold`` keyword, which is controlled via
        the ``[units.quantity]`` configuration item ``latex_array_threshold``.
        This is treated separately because the numpy default of 1000 is too big
        for most browsers to handle.

        Parameters
        ----------
        unit : unit-like, optional
            Specifies the unit.  If not provided,
            the unit used to initialize the quantity will be used.

        precision : number, optional
            The level of decimal precision. If `None`, or not provided,
            it will be determined from NumPy print options.

        format : str, optional
            The format of the result. If not provided, an unadorned
            string is returned. Supported values are:

            - 'latex': Return a LaTeX-formatted string

            - 'latex_inline': Return a LaTeX-formatted string that uses
              negative exponents instead of fractions

        subfmt : str, optional
            Subformat of the result. For the moment, only used for
            ``format='latex'`` and ``format='latex_inline'``. Supported
            values are:

            - 'inline': Use ``$ ... $`` as delimiters.

            - 'display': Use ``$\\displaystyle ... $`` as delimiters.

        Returns
        -------
        str
            A string with the contents of this Quantity
        """
        ...
    
    def __str__(self) -> str:
        ...
    
    def __repr__(self): # -> str:
        ...
    
    def __format__(self, format_spec): # -> str:
        """
        Format quantities using the new-style python formatting codes
        as specifiers for the number.

        If the format specifier correctly applies itself to the value,
        then it is used to format only the value. If it cannot be
        applied to the value, then it is applied to the whole string.

        """
        ...
    
    def decompose(self, bases=...): # -> Self@Quantity | Quantity | Any:
        """
        Generates a new `Quantity` with the units
        decomposed. Decomposed units have only irreducible units in
        them (see `astropy.units.UnitBase.decompose`).

        Parameters
        ----------
        bases : sequence of `~astropy.units.UnitBase`, optional
            The bases to decompose into.  When not provided,
            decomposes down to any irreducible units.  When provided,
            the decomposed result will only contain the given units.
            This will raises a `~astropy.units.UnitsError` if it's not possible
            to do so.

        Returns
        -------
        newq : `~astropy.units.Quantity`
            A new object equal to this quantity with units decomposed.
        """
        ...
    
    def item(self, *args): # -> Self@Quantity | Quantity | Any:
        """Copy an element of an array to a scalar Quantity and return it.

        Like :meth:`~numpy.ndarray.item` except that it always
        returns a `Quantity`, not a Python scalar.

        """
        ...
    
    def tolist(self):
        ...
    
    def itemset(self, *args): # -> None:
        ...
    
    def tostring(self, order=...):
        ...
    
    def tobytes(self, order=...):
        ...
    
    def tofile(self, fid, sep=..., format=...):
        ...
    
    def dump(self, file):
        ...
    
    def dumps(self):
        ...
    
    def fill(self, value): # -> None:
        ...
    
    @property
    def flat(self): # -> QuantityIterator:
        """A 1-D iterator over the Quantity array.

        This returns a ``QuantityIterator`` instance, which behaves the same
        as the `~numpy.flatiter` instance returned by `~numpy.ndarray.flat`,
        and is similar to, but not a subclass of, Python's built-in iterator
        object.
        """
        ...
    
    @flat.setter
    def flat(self, value): # -> None:
        ...
    
    def take(self, indices, axis=..., out=..., mode=...): # -> Self@Quantity | Quantity | Any:
        ...
    
    def put(self, indices, values, mode=...): # -> None:
        ...
    
    def choose(self, choices, out=..., mode=...):
        ...
    
    def argsort(self, axis=..., kind=..., order=...): # -> ndarray[Unknown, Unknown]:
        ...
    
    def searchsorted(self, v, *args, **kwargs): # -> NDArray[intp]:
        ...
    
    def argmax(self, axis=..., out=...): # -> intp:
        ...
    
    def argmin(self, axis=..., out=...): # -> intp:
        ...
    
    def __array_function__(self, function, types, args, kwargs): # -> Any | _NotImplementedType | tuple[Unknown, ...] | list[Unknown] | Self@Quantity | Quantity:
        """Wrap numpy functions, taking care of units.

        Parameters
        ----------
        function : callable
            Numpy function to wrap
        types : iterable of classes
            Classes that provide an ``__array_function__`` override. Can
            in principle be used to interact with other classes. Below,
            mostly passed on to `~numpy.ndarray`, which can only interact
            with subclasses.
        args : tuple
            Positional arguments provided in the function call.
        kwargs : dict
            Keyword arguments provided in the function call.

        Returns
        -------
        result: `~astropy.units.Quantity`, `~numpy.ndarray`
            As appropriate for the function.  If the function is not
            supported, `NotImplemented` is returned, which will lead to
            a `TypeError` unless another argument overrode the function.

        Raises
        ------
        ~astropy.units.UnitsError
            If operands have incompatible units.
        """
        ...
    
    def trace(self, offset=..., axis1=..., axis2=..., dtype=..., out=...): # -> tuple[Unknown, ...] | list[Unknown] | Self@Quantity | Quantity | Any:
        ...
    
    if NUMPY_LT_1_20:
        def var(self, axis=..., dtype=..., out=..., ddof=..., keepdims=...): # -> tuple[Unknown, ...] | list[Unknown] | Self@Quantity | Quantity | Any:
            ...
        
    else:
        def var(self, axis=..., dtype=..., out=..., ddof=..., keepdims=..., *, where=...): # -> tuple[Unknown, ...] | list[Unknown] | Self@Quantity | Quantity | Any:
            ...
        
    if NUMPY_LT_1_20:
        def std(self, axis=..., dtype=..., out=..., ddof=..., keepdims=...): # -> tuple[Unknown, ...] | list[Unknown] | Self@Quantity | Quantity | Any:
            ...
        
    else:
        def std(self, axis=..., dtype=..., out=..., ddof=..., keepdims=..., *, where=...): # -> tuple[Unknown, ...] | list[Unknown] | Self@Quantity | Quantity | Any:
            ...
        
    if NUMPY_LT_1_20:
        def mean(self, axis=..., dtype=..., out=..., keepdims=...): # -> tuple[Unknown, ...] | list[Unknown] | Self@Quantity | Quantity | Any:
            ...
        
    else:
        def mean(self, axis=..., dtype=..., out=..., keepdims=..., *, where=...): # -> tuple[Unknown, ...] | list[Unknown] | Self@Quantity | Quantity | Any:
            ...
        
    def round(self, decimals=..., out=...): # -> tuple[Unknown, ...] | list[Unknown] | Self@Quantity | Quantity | Any:
        ...
    
    def dot(self, b, out=...): # -> tuple[Unknown, ...] | list[Unknown] | Self@Quantity | Quantity | Any:
        ...
    
    def all(self, axis=..., out=...):
        ...
    
    def any(self, axis=..., out=...):
        ...
    
    def diff(self, n=..., axis=...): # -> tuple[Unknown, ...] | list[Unknown] | Self@Quantity | Quantity | Any:
        ...
    
    def ediff1d(self, to_end=..., to_begin=...): # -> tuple[Unknown, ...] | list[Unknown] | Self@Quantity | Quantity | Any:
        ...
    
    if NUMPY_LT_1_22:
        def nansum(self, axis=..., out=..., keepdims=...): # -> tuple[Unknown, ...] | list[Unknown] | Self@Quantity | Quantity | Any:
            ...
        
    else:
        def nansum(self, axis=..., out=..., keepdims=..., *, initial=..., where=...): # -> tuple[Unknown, ...] | list[Unknown] | Self@Quantity | Quantity | Any:
            ...
        
    def insert(self, obj, values, axis=...): # -> Self@Quantity | Quantity | Any:
        """
        Insert values along the given axis before the given indices and return
        a new `~astropy.units.Quantity` object.

        This is a thin wrapper around the `numpy.insert` function.

        Parameters
        ----------
        obj : int, slice or sequence of int
            Object that defines the index or indices before which ``values`` is
            inserted.
        values : array-like
            Values to insert.  If the type of ``values`` is different
            from that of quantity, ``values`` is converted to the matching type.
            ``values`` should be shaped so that it can be broadcast appropriately
            The unit of ``values`` must be consistent with this quantity.
        axis : int, optional
            Axis along which to insert ``values``.  If ``axis`` is None then
            the quantity array is flattened before insertion.

        Returns
        -------
        out : `~astropy.units.Quantity`
            A copy of quantity with ``values`` inserted.  Note that the
            insertion does not occur in-place: a new quantity array is returned.

        Examples
        --------
        >>> import astropy.units as u
        >>> q = [1, 2] * u.m
        >>> q.insert(0, 50 * u.cm)
        <Quantity [ 0.5,  1.,  2.] m>

        >>> q = [[1, 2], [3, 4]] * u.m
        >>> q.insert(1, [10, 20] * u.m, axis=0)
        <Quantity [[  1.,  2.],
                   [ 10., 20.],
                   [  3.,  4.]] m>

        >>> q.insert(1, 10 * u.m, axis=1)
        <Quantity [[  1., 10.,  2.],
                   [  3., 10.,  4.]] m>

        """
        ...
    


class SpecificTypeQuantity(Quantity):
    """Superclass for Quantities of specific physical type.

    Subclasses of these work just like :class:`~astropy.units.Quantity`, except
    that they are for specific physical types (and may have methods that are
    only appropriate for that type).  Astropy examples are
    :class:`~astropy.coordinates.Angle` and
    :class:`~astropy.coordinates.Distance`

    At a minimum, subclasses should set ``_equivalent_unit`` to the unit
    associated with the physical type.
    """
    _equivalent_unit = ...
    _unit = ...
    _default_unit = ...
    __array_priority__ = ...
    def __quantity_subclass__(self, unit): # -> tuple[Type[Self@SpecificTypeQuantity], Literal[True]] | tuple[Type[Quantity], Literal[False]]:
        ...
    


def isclose(a, b, rtol=..., atol=..., equal_nan=..., **kwargs): # -> bool_:
    """
    Return a boolean array where two arrays are element-wise equal
    within a tolerance.

    Parameters
    ----------
    a, b : array-like or `~astropy.units.Quantity`
        Input values or arrays to compare
    rtol : array-like or `~astropy.units.Quantity`
        The relative tolerance for the comparison, which defaults to
        ``1e-5``.  If ``rtol`` is a :class:`~astropy.units.Quantity`,
        then it must be dimensionless.
    atol : number or `~astropy.units.Quantity`
        The absolute tolerance for the comparison.  The units (or lack
        thereof) of ``a``, ``b``, and ``atol`` must be consistent with
        each other.  If `None`, ``atol`` defaults to zero in the
        appropriate units.
    equal_nan : `bool`
        Whether to compare NaN’s as equal. If `True`, NaNs in ``a`` will
        be considered equal to NaN’s in ``b``.

    Notes
    -----
    This is a :class:`~astropy.units.Quantity`-aware version of
    :func:`numpy.isclose`. However, this differs from the `numpy` function in
    that the default for the absolute tolerance here is zero instead of
    ``atol=1e-8`` in `numpy`, as there is no natural way to set a default
    *absolute* tolerance given two inputs that may have differently scaled
    units.

    Raises
    ------
    `~astropy.units.UnitsError`
        If the dimensions of ``a``, ``b``, or ``atol`` are incompatible,
        or if ``rtol`` is not dimensionless.

    See also
    --------
    allclose
    """
    ...

def allclose(a, b, rtol=..., atol=..., equal_nan=..., **kwargs) -> bool:
    """
    Whether two arrays are element-wise equal within a tolerance.

    Parameters
    ----------
    a, b : array-like or `~astropy.units.Quantity`
        Input values or arrays to compare
    rtol : array-like or `~astropy.units.Quantity`
        The relative tolerance for the comparison, which defaults to
        ``1e-5``.  If ``rtol`` is a :class:`~astropy.units.Quantity`,
        then it must be dimensionless.
    atol : number or `~astropy.units.Quantity`
        The absolute tolerance for the comparison.  The units (or lack
        thereof) of ``a``, ``b``, and ``atol`` must be consistent with
        each other.  If `None`, ``atol`` defaults to zero in the
        appropriate units.
    equal_nan : `bool`
        Whether to compare NaN’s as equal. If `True`, NaNs in ``a`` will
        be considered equal to NaN’s in ``b``.

    Notes
    -----
    This is a :class:`~astropy.units.Quantity`-aware version of
    :func:`numpy.allclose`. However, this differs from the `numpy` function in
    that the default for the absolute tolerance here is zero instead of
    ``atol=1e-8`` in `numpy`, as there is no natural way to set a default
    *absolute* tolerance given two inputs that may have differently scaled
    units.

    Raises
    ------
    `~astropy.units.UnitsError`
        If the dimensions of ``a``, ``b``, or ``atol`` are incompatible,
        or if ``rtol`` is not dimensionless.

    See also
    --------
    isclose
    """
    ...

